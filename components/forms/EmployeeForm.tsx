"use client";

/**
 * EmployeeForm — Stage 2 placeholder
 * This component will be built in Stage 2 (Form UI Improvement).
 * It will replace the inline form in app/employee/page.tsx
 * with reusable AppInput/AppSelect/AppButton components.
 */

export default function EmployeeForm() {
  return (
    <div className="p-8 text-center text-sm text-[var(--color-neutral-400)]">
      Employee form will be built in Stage 2.
    </div>
  );
}
